services:
  snail:
    build:
      context: .
      dockerfile: Dockerfile.headless
    working_dir: /snail
    volumes:
      - .:/snail
    ports:
      - "${HTTP_PORT:-8080}:${HTTP_PORT:-8080}"
    command: ["sh", "watch.sh"]
